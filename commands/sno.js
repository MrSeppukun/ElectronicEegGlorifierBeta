module.exports = {
    name: 'sno',
    args: false,
    type: 'personal',

    execute(message, args) {
        message.channel.send('the other kid, i think its a girl (cringe warning)');
    }
}