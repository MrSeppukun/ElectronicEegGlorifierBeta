const Discord = require('discord.js');

module.exports = {
    name: 'inv',
    args: false,
    description : 'Displays everything in your inventory',
    type: 'currency',

    async execute(message, args, inventoryList, quantityList) {

    }
}